# 1. Database Definition
databases:
  - name: gateway-db-prod
    databaseName: payment_gateway
    user: gateway_user
    plan: free
    region: ohio 

# 2. Services Definition
services:
  # FastAPI Backend (Web Service)
  - type: web
    name: gateway-api-service
    env: docker
    dockerContext: ./backend
    dockerfilePath: ./backend/Dockerfile
    plan: free
    region: ohio 
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: gateway-db-prod
          # CHANGED: Using connectionString (External) instead of internal DNS
          property: connectionString
      - key: PORT
        value: "8000"
      - key: TEST_MODE
        value: "true"
      - key: TEST_PAYMENT_SUCCESS
        value: "true"
      - key: TEST_PROCESSING_DELAY
        value: "1000"
      - key: MERCHANT_API_KEY
        value: key_test_abc123
      - key: MERCHANT_API_SECRET
        value: secret_test_xyz789

  # Merchant Dashboard (Static Site)
  - type: web
    name: merchant-dashboard-ui
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_API_URL
        value: https://gateway-api-service.onrender.com

  # Hosted Checkout (Static Site)
  - type: web
    name: checkout-page-ui
    runtime: static
    rootDir: checkout-page
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      - key: REACT_APP_API_URL
        value: https://gateway-api-service.onrender.com